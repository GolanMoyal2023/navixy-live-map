// Local: when you open the map from this PC (127.0.0.1:8080 or localhost).
window.NAVIXY_MAP_API_BASE = "http://127.0.0.1";
// Public: when the map is opened from GitHub Pages (e.g. on mobile). Must be reachable from the internet.
window.NAVIXY_MAP_API_BASE_PUBLIC = "https://navixy-livemap.moyals.net";
